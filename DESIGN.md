# Walk UP! 設計仕様書

RevenueCat Shipaton 2026 提出作品 / 万歩計RPG
最終更新: 2026-07-25

| 項目 | 値 | 一意性 |
|---|---|---|
| App Store の名前 | **Walk UP! 万歩計RPG** | 全世界で重複不可 |
| ホーム画面の表示名 | **Walk UP!** — Wake up × Walk。眠った世界を歩いて起こす | 制約なし |
| Bundle ID | **`com.akaoonaga.walkup`**（変更不可・確定済み） | 全世界で重複不可 |
| Xcode プロジェクト | `WalkUP.xcodeproj` | — |
| Team ID | `C3QY49MZ54`（Apple Developer Program / 個人登録） | — |

「Walk Up!」単体は既に他社アプリが使用済みだったため、ストア名にキーワードを付与して一意性を確保した。
ホーム画面の表示名には一意性の制約がないため `Walk UP!` を維持している（`INFOPLIST_KEY_CFBundleDisplayName`）。
「万歩計」「RPG」はストア内検索の需要が大きい語なので、一意性の確保と ASO を兼ねている。

---

## 0. 提出要件（絶対条件）

| 項目 | 内容 |
|---|---|
| 提出期間 | 2026/8/1 〜 9/30 |
| 必須 | 新規アプリを期間中に **初回リリース**（8/1より前に公開したら失格） |
| 必須 | RevenueCat SDK で最低1つの課金を実装 |
| 審査 | 10/1〜10/13、結果発表 10/21 |
| 加点 | Build in Public（期間中の公開進捗発信） |

TestFlight 配布はリリースに当たらないため 7月中でも可。**App Store 公開だけは 8/1 以降。**

### ✅ 課金の前提条件（2026-07-25 確認済み・クリア）

Shipaton の課金要件を満たすには App内課金を作成できる状態が必要だが、以下は全て有効であることを確認済み。
**待ち時間リスクなし。App内課金をいつでも作成できる。**

| 項目 | ステータス | 有効期間 |
|---|---|---|
| 有料アプリ契約 | 有効 | 2026/5/7 – 2027/5/7 |
| 無料アプリ契約 | 有効 | 2026/7/25 – 2027/5/7 |
| 銀行口座（ゆうちょ銀行） | 有効 | JPY 受取／ロイヤルティ USD |
| 納税フォーム（W-8BEN 等） | 有効 | 2023/12/25 提出 |
| デジタルサービス法（EU 27か国） | 有効 | 2026/5/8 更新 |

有料アプリ契約の期限は 2027/5/7 で Shipaton 全期間をカバーする。EU 配信も DSA 対応済みのため地域を絞る必要はない。

### 個人登録に伴う注意

登録タイプが「個人」のため、App Store 上の販売者名には**登録者の氏名が表示される**。
屋号での表示を希望する場合は、別途対応が必要（後回しでよいが、公開前に確認すること）。

---

## 1. コンセプト

世界は「**ダルモン**」によって怠惰に堕とされ、人々が動かなくなった。
歩数だけが、その怠惰を打ち破る唯一の資源である。

この一文が本作の設計原理。「歩く」という現実の行動が、そのまま物語上の行動になっている点が
既存の万歩計RPG（歩数→数値→強くなる、の恣意的な接続）との差別化点。

- **ストーリーモード**（有限・作家性）: ダルモンを討伐し、地域を解放する
- **やり込み層**（無限・継続装置）: 解放した地域の「活気」を取り戻す

### 1.1 主人公の武器は靴（2026-07-26 確定）

**主人公の名前は「ウォーク」**（2026-07-27 確定）。声は**熱血**、一人称は「俺」。
止まった世界の中で**一人だけ熱い**という対比で立たせる。
「ウォーク力」を口に出させてよいのはセリフの中だけで、地の文とUIには出さない。
それまで画面には「主人公」とだけ出しており、名前は決まっていなかった。

**名前をどこで名乗らせるかは物語側で決める。** UI の名札を機械的に置き換えないこと。
戦闘の計器が先に「ウォーク」と表示してしまうと、物語で名前が出る場面が死ぬ。
当面 UI は「主人公」のまま。

**ただし §6.2 の施設「ウォークの軌跡」は第1章の活気45%で開く。**
章クリア＋周回3日ほどで届くので、**そこまでに名前が出ていないと施設名が初出になる。**
名前を出す場面は、それより手前に置くこと。

主人公は剣も杖も持たない。**武器スロットに入るのは靴**であり、
振るうのは足である。この力を作中では「**ウォーク力**」と呼ぶ。

上の一文（歩数だけが怠惰を打ち破る唯一の資源）からの必然。
手に持つ武器を与えると、「歩いて強くなる」筋と「武器で戦う」筋の2本が並走し、
歩数はただの経験値供給源に落ちる。**現実の行動が物語上の行動である**という
差別化点そのものが薄まる。

実装への波及:

- `MasterData.equipmentName()` の武器は「目覚めの靴 / 抗いの靴 / 灯火の靴」
- **`EquipmentSlot.weapon` の内部名は変えない。** 保存データの装備 ID
  （`eq_ch1_weapon`）に入っており、変えると既存プレイヤーの装備が消える
- 主人公のアートは杖を外して靴を描き込む（`ART_PROMPTS.md` §3.8）
- 「ウォーク力」はコピーとして使う語で、ステータス名ではない。
  攻撃力の表示は「攻」のまま（狭い行に収めるため）

---

## 2. コアループ

```
昼: 歩く
 └→ 歩数が経験値と活力(AP)に変換される

夜: アプリを開く（1セッション3分）
 ├→ レベルアップ（自動・必ず伸びる）
 ├→ 装備を選ぶ / 強化する      ← ここが唯一の意思決定
 ├→ 活力を使って戦う（自動戦闘・演出）
 └→ 素材と図鑑が増える
```

**設計上の絶対ルール**

1. 歩けなかった日に**罰を与えない**。APが少ないだけで、レベルは減らない。
2. 課金で**歩数を買わせない**。アプリの前提を否定する。
3. 課金で**ストーリーの続きを売らない**。「歩いたのに進めない」は最悪の体験。
4. 生成・計算は**アプリを開いた時のみ**行う。休眠ユーザーにコストをかけない。

---

## 3. 歩数 → ゲーム資源の変換

### 3.1 二層構造

```
1日の歩数（上限 30,000歩 ＝ チート対策）
  ├─→ 経験値   : 1歩 = 1 EXP    → レベル → 基礎ステータス（自動・安心感）
  └─→ 活力(AP) : 1,000歩 = 1 AP → 討伐回数 → 素材 → 装備（選択・深さ）
```

歩けば必ずレベルが上がるので「今日も無駄じゃなかった」が保証される。
その上に装備という選択の層が乗る。

### 3.2 レベル曲線

```
Lv N に必要な累計EXP = 500 × N²
```

| Lv | 累計歩数 | 到達目安 |
|---|---|---|
| 2 | 2,000 | 初日 |
| 5 | 12,500 | 2日目 |
| 6 | 18,000 | 3日目（第1章クリア想定） |
| 10 | 50,000 | 8日目 |
| 15 | 112,500 | 16日目 |
| 20 | 200,000 | 30日目 |

### 3.3 基礎ステータス

```
HP  = 50 + Lv × 10 + Σ(装備HP)
ATK = 10 + Lv ×  3 + Σ(装備ATK)
DEF =  5 + Lv ×  2 + Σ(装備DEF)
```

### 3.4 二重計上の防止（実装上の要注意点）

`PlayerState.lastProcessedDate` と `todayCreditedSteps` を保持し、

- **過去日**: `lastProcessedDate` より後の日別バケットのみを加算し、日付を進める
- **当日**: `min(今日の歩数, 30000) - todayCreditedSteps` の差分だけ加算し、`todayCreditedSteps` を更新

日付変更をまたぐ場合・端末の日付を巻き戻された場合の両方を必ずテストすること。

---

## 4. 戦闘仕様

### 4.1 方針

完全自動戦闘。**意思決定は戦闘の「前」に全部置く。**
戦闘そのものは 3〜8 秒のログ再生演出であり、勝敗は準備段階で決まっている。

| モード | 意思決定 |
|---|---|
| ストーリー | 装備の選択 / 強化素材の配分（相手は固定） |
| やり込み | 上記 ＋ **挑む相手の選択**（今日のAPをどこに使うか） |

### 4.2 計算式

```
ダメージ = max(1, ATK - DEF / 2) × random(0.8 ... 1.2)
```

**乱数幅は当初 ±10%（0.9〜1.1）だったが、シミュレーションの結果 ±20% に改めた（§17.12）。**
±10% では10ターン以上の戦闘で振れが平均に収束し、勝敗が実質的に決定論になる。

- 先攻は主人公固定（シンプルさ優先）
- ターン交互、どちらかの HP が 0 で終了
- 30ターンで決着しない場合は敗北扱い（「活力が尽きて撤退」）

### 4.3 敗北時の扱い

全損させない。**素材の一部は持ち帰れる。** 消費した AP は返らない。
敗北画面には必ず「装備を強化する」への導線を置く。
「準備不足で負けた → 整えて出直す」という因果をプレイヤーに理解させることが目的。

### 4.4 バランス検証例（第1章ボス）

| | HP | ATK | DEF |
|---|---|---|---|
| 第1章ボス | 300 | 25 | 10 |
| 主人公 Lv6・装備なし | 110 | 28 | 17 |
| 主人公 Lv6・装備あり | 160 | 38 | 22 |

- **装備なし**: 与 23/T → 14ターン必要。被 16.5/T → 7ターンで死亡 → **敗北**
- **装備あり**: 与 33/T → 10ターン必要。被 14/T → 12ターン耐える → **辛勝**

装備を整えれば勝てるが、素の成長だけでは勝てない。この差分がゲーム性の本体。
全ボスをこの形式で検証してから実装すること。

---

## 5. ストーリーモード

### 5.1 章構成（MVP は3章）

| 章 | 累計歩数ゲート | 到達目安 | 内容 |
|---|---|---|---|
| 第1章 | 20,000 | 約3日 | チュートリアル。最初のダルモン撃破 |
| 第2章 | 60,000 | 約9日 | 装備・強化システム解放 |
| 第3章 | 120,000 | 約18日 | ダルモン本体戦。本編完結 |

各章は 8〜10 の**ノード**（固定エンカウント）で構成。ノード1つ = おおむね1日分の歩数。

### 5.2 序盤を極端に軽くする理由

Shipaton の審査期間は 10/1〜10/13 で、**審査員は歩かない**。
長大なストーリーは審査で1ミリも評価されない。初日〜2日目で最初のボスを倒せることが最優先。
長編化はリリース後のアップデートで行う。

### 5.3 完結感の担保

第3章でダルモン本体は倒すが、世界の完全復興は達成されない。
→ そのままやり込み層（復興）へ接続する。

---

## 6. やり込み層

本編クリア後の継続装置。**本編と同時に作る。**後回しにするとクリア＝離脱が直撃する。

| 要素 | 内容 |
|---|---|
| 討伐 | 解放済み地域にダルモン残党が日替わりで出現。APを消費して周回 |
| 復興 | 地域ごとの「活気ゲージ」。討伐で上昇。上がると施設・素材が解放 |
| 育成 | 素材で装備を強化。歩数の使い道が常にある |
| 図鑑 | 歩行データ（時間帯・距離・階数）で出現するダルモンが変わる → 収集動機 |

「倒す」だけでなく「**取り戻す**」を継続の軸にすることで、テーマとも一致する。

### 6.1 活気の節目と施設（2026-07-27 確定）

**物語の背骨は「街の人々を起こしていく」。** 人々が起きると街が活気を取り戻し、
施設が開く。活気ゲージ（§17.9）は飾りではなく**物語の目盛りそのもの**。

演出の半分は既に動いている。`worldLayers` は活気で復興後の背景をクロスフェードし、
灯りを段階的に点し、空気の粒を暖色に変える。**足りないのは言葉だけ。**
場面は「背景が既に変わる瞬間」に置く。

| 節目 | 起きること |
|---|---|
| 12% | 場面。最初の人が起きる（灯りが1つ点く） |
| **45%** | 場面 ＋ **その地域の施設が開く** |
| 75% | 場面（灯りが増える） |
| **100%** | 場面 ＋ **施設の効果が伸びる**。その地域は戻った |

3地域 × 4節目 = **12場面**。閾値は既存の灯りの段階（12/45/75）に 100 を足したもので、
**新しい閾値を作らない**（背景の変化と言葉が同じ瞬間に起きる）。

到達の実測: 章を全クリアで活気24（雑魚7×2＋ボス10）。100 までは周回で
雑魚38回 ≒ 38,000歩 ≒ 9.5日（4,000歩/日）。**活気は章の進行では埋まらず、周回で埋まる。**
つまり施設はクリア後の遊びとして開く。審査員は到達しない。

### 6.2 施設は地図の上に建てる

**帳面にしない。** 施設は街に在る物なので、討伐画面の地図に標として置く。
§3 の「一覧を行で組まない、地図にする」と同じ理由で、ホームの衛星ボタンも増やさない。

| 地域 | 施設 | 45% | 100% |
|---|---|---|---|
| 第1章 止まった住宅街 | **ウォークの軌跡** | 歩き方を靴に刻める（核1つ） | 刻印が2つ載る |
| 第2章 灯りの落ちた商店街 | **市** | 素材を交換できる（**核の使い道**） | 交換率が良くなる |
| 第3章 灰の窪地 | **掲示板** | 日替わりの残党が1体貼り出される | 2体になる |

**「工房」は活気の施設ではない。** 澱で靴を強化する場所で、**チュートリアルで開く**（§11.1）。
強化はゲーム開始直後から要る仕組みなので、周回でしか届かない活気に紐づけられない。

#### 工房（チュートリアルで開く）

既存の「装備・強化」がこれにあたる。名前を与えて場所にしただけで、仕組みは変えない。
チュートリアルの6段目で澱6が貯まり、そこで開く。

#### 掲示板（§6 の「日替わり残党」の規則）

その日に貼られる相手は、**解放済み地域の個体から日替わりで1体**。
選び方は日付を種にした決定的な抽選にする（同じ日に開き直しても変わらない）。
報酬は通常の1.5倍。**活力の消費は通常どおり**（ここを無料にすると歩数の意味が薄れる）。

**依頼の一覧を作らないこと。** 縦に積んだデイリークエストは習慣化アプリの語彙そのもので、
`web/README.md` §1 が書いた失敗を仕組みの側から呼び戻す。
地図の上に**貼り紙が1枚ある**という形に留める。

#### 市（核の使い道）

| 出す物 | もらう物 |
|---|---|
| 核 1 | 澱 40（100% で 60） |
| 澱 30 | かけら 5（100% で 8） |

§17.7 の「最大強化まで275澱」に対し、核1つ＝40澱は**1.5段ぶん**。
ボスと道標からしか出ない核を、育成へ流す経路になる。
**歩数は買えないし、売れない**（§2 の絶対ルール2）。

#### ウォークの軌跡（§6 の「歩行データ」）

§6 は歩行データで**出現するダルモンを変える**としていたが、
図鑑の収集動機としては弱い（プレイヤーは出現表を知らないので、変わったことに気付けない）。
**歩き方が装備になる**方に振り替える。変化が自分の数値として返るので、気付ける。

名前は主人公の名（ウォーク）から。**歩いた軌跡そのものが装備になる**場所なので、
「工房」（澱で打ち直す場所）とは役割が違うことが名前で分かる。

| 刻印 | 条件（直近7日で最も強い傾向） | 効果 |
|---|---|---|
| 朝の刻印 | 早朝の歩数の割合が高い | ATK +4 |
| 坂の刻印 | 上った階数が多い | DEF +4 |
| 遠出の刻印 | 1日の最長距離が長い | HP +30 |

核1つで刻み直せる。効果量は強化1段ぶんに揃えてある。
**ブラウザ版には歩行データが無い**ので、開発用トレイから歩き方を指定する
（本編では HealthKit の時間帯別歩数・階数・距離から導く）。

---

## 7. 課金設計（RevenueCat）

### 7.1 SKU 一覧

| Product ID | 種別 | 価格（想定） | 内容 |
|---|---|---|---|
| `walkup_pass_monthly` | 自動更新サブスク | ¥480/月 | **本命**。やり込み層の報酬トラック強化。**登録済み** |
| `walkup_skin_pack_01` | 買い切り | ¥360 | 装備スキン。ゲームバランスに非干渉。未登録 |
| `walkup_step_recovery` | 消費型 | ¥120 | 3日前まで遡って歩数を回収。未登録 |

RevenueCat Entitlement: `pass`（表示名「活力パス」）

接頭辞は `walque_` ではなく **`walkup_`**。App Store Connect と RevenueCat には
`walkup_` で登録済みなので、以後この表記に統一する。
アプリ側の参照は `WalkUP/Purchases/PurchaseIdentifiers.swift` に集約している。

### 登録済みの識別子（2026-07-25 時点）

| 項目 | 値 |
|---|---|
| App Store Connect App ID | `6794604332` |
| サブスクリプショングループ | `Walk UP Pass`（ID `22263431`） |
| サブスクリプション Apple ID | `6794621905` |
| 製品ID | `walkup_pass_monthly`（1か月・¥480・175の国と地域） |
| 表示名 / 説明 | 活力パス / ダルモン討伐の報酬が増え、復興が早く進む月額パス。 |
| RevenueCat Project ID | `86f6ac0c` |
| RevenueCat App ID | `appce2026adfe` |
| RevenueCat Entitlement | `pass`（REST ID `entlbfd2b34887`） |
| RevenueCat Offering | `default`（REST ID `ofrnge1a1926a25`） |

### ダッシュボード設定の完了状況

| 項目 | 状態 |
|---|---|
| App Store Connect API キー | ✅ Valid credentials |
| In-App Purchase キー | ✅ Valid credentials |
| 商品インポート | ✅ `walkup_pass_monthly` |
| Entitlement `pass` → 商品 | ✅ 紐付け済み |
| Offering `default` → `$rc_monthly` | ✅ `walkup_pass_monthly` を紐付け済み |

**In-App Purchase キーは、商品を1つも作っていない間は `Credentials need attention` になる。**
入力値の誤りではないので、この段階で慌ててキーを作り直さないこと。商品作成後に自動で解消する。

`$rc_annual` / `$rc_lifetime` パッケージには Test Store の商品のみが残っている。
対応する App Store 商品が無いため実害はないが、不要になったら削除してよい。

### 未完了（次にやること）

1. ~~RevenueCat の確認メール~~ ✅ 完了
2. ~~RevenueCat SDK（`purchases-ios-spm`）を Xcode に追加~~ ✅ 完了（5.81.2、`upToNextMajor`）
3. ~~ペイウォール画面の実装~~ ✅ 完了（`WalkUP/Purchases/PaywallView.swift`）
4. ~~Public API Key を `Secrets.xcconfig` に記入~~ ✅ 完了（SDK 初期化・サーバー到達を確認済み）
5. ~~プライバシーポリシーの本文を用意する~~ ✅ 完了（`docs/privacy-policy.html`・日英併記）
6. ~~TestFlight へ初回ビルドをアップロード~~ ✅ 完了（0.1.0 / build 1・2026-07-26）
7. ~~GitHub Pages を有効化~~ ✅ 完了（URL は HTTP 200 を返す）
8. ~~App Store Connect にプライバシーポリシー URL を登録~~ ✅ 完了（2026-07-27）
   「アプリのプライバシー」→ プライバシーポリシー に登録済み。アプリ内リンクと同一。
9. **Sandbox での購入・復元の実機確認**（未了・下記 7.4 の状況）
10. 最初のサブスクリプショングループは**新しいアプリバージョンとともに審査提出する必要がある**（App Store Connect の注意書き）

### 7.4 商品が取得できない問題（2026-07-26 時点・未解決）

実機・シミュレータのいずれでも、Offering の取得が
**`RevenueCat.ErrorCode error 23`（`configurationError`）** で失敗する。
ペイウォールには「現在この商品を購入できません」が表示される。

切り分けの結果、**アプリの実装側に問題は無い**ことが確定している。

| 除外できた原因 | 根拠 |
|---|---|
| Sandbox アカウント | 未サインインのシミュレータでも同一エラー |
| 実機／端末設定 | 同上 |
| API キー | 誤っていれば `invalidCredentialsError` になる。SDK 初期化とサーバー到達は成功している |
| ペイウォール実装 | エラーは商品取得段階。UI に到達していない |
| ASC のメタデータ不足 | ステータス確認済み（不足なし） |

残る原因は **StoreKit が App Store Connect から `walkup_pass_monthly` を引けていない**こと。

### 切り分けの決着（2026-07-26 04:49）

RevenueCat を迂回して StoreKit へ直接照会したところ、**商品 0 件**だった。
これにより原因は Apple 側に確定し、**RevenueCat の設定は無関係**と判明した。

```
storefront: JPN
StoreKit.Product.products(for: ["walkup_pass_monthly"]) → 0件
```

同時に潰れた容疑:

| 項目 | 結果 | 根拠 |
|---|---|---|
| Sandbox アカウントの国 | 正常 | storefront が `JPN` |
| Bundle ID の一致 | 正常 | **TestFlight のアップロードが通った**（不一致なら弾かれる） |
| RevenueCat の Offering / 商品紐付け | 無関係 | StoreKit 直接照会でも 0 件 |
| ビルド未アップロード | **仮説は外れ** | アップロード・処理完了後も 0 件のまま |

### 原因の特定（2026-07-27）

**アプリ本体の「価格および配信状況」が未設定だった。**

| 項目 | 状態 |
|---|---|
| 価格表 | 「初期価格を設定してください」= **未設定** |
| アプリの配信状況 | 「配信状況の設定」= **配信国が未設定** |

**アプリ自体が配信対象になっていないと、StoreKit はそのアプリ内課金を返さない。**
無料アプリでも価格（¥0）と配信国の設定は必要。サブスクリプション側は
価格（175の国と地域）もローカリゼーションも揃っていたため、
商品だけを見ていて気付けなかった。

先に潰れた仮説:

| 仮説 | 結果 |
|---|---|
| 商品情報の伝播待ち | **外れ**。登録から2日経っても 0 件のまま |
| 契約の未同意項目 | **外れ**。有料アプリ契約・銀行口座・納税フォームすべて「有効」 |
| ローカルの StoreKit 設定ファイル | **外れ**。スキームに StoreKit 設定は入っていない |

### 残っている作業（ユーザー操作が必要）

App Store Connect のモーダルは自動操作で開けなかったため、以下は手作業。

1. **価格および配信状況** → 「価格を追加」で **¥0（無料）**、
   「配信状況の設定」で配信国を選ぶ（サブスクリプションに合わせるなら175の国と地域）
2. **サブスクリプショングループのローカリゼーション**が未作成。
   グループ表示名（例: 活力パス）を1件作る。審査提出前に必須
3. **サブスクリプションの審査用スクリーンショット**が未添付。提出前に必須
4. 1 の反映後、ペイウォールの診断で商品が取れるか確認 → Sandbox で購入と復元

**この段階で実装をいじらないこと。** 原因が Apple 側にある以上、手を入れても直らず、
動き出した時に何が効いたのか分からなくなる。課金実装は完了扱いとし、
コアループの実装へ進んでよい。

診断は `WalkUP/Purchases/PaywallView.swift` の `diagnostics`（`#if DEBUG` 限定）が
ペイウォール下部に表示する。macOS 26 の `log stream` は実機ログの取得に対応しなくなっており、
外からログを読む手段が無いため画面に出している。

### 7.2 法務・公開情報

| 項目 | 値 |
|---|---|
| プライバシーポリシー | `docs/privacy-policy.html` → https://akao-onaga.github.io/WalkUP/privacy-policy.html |
| 配信方法 | GitHub Pages（`main` / `docs`）。`docs/.nojekyll` により HTML をそのまま配信 |
| 利用規約 | Apple 標準 EULA（独自 EULA は用意しない） |
| 公開連絡先 | `akao.gamedev@gmail.com`（ASC のサポート連絡先とも揃えること） |

**ポリシー本文の要点**: ヘルスケアの歩数は端末外に出ない。一方で購入情報（匿名ID・購入履歴・
端末情報・IP）は RevenueCat（米国）に送信され、**課金しない利用者でも商品情報の取得で通信が発生する**。
ここを省くと ASC の「App のプライバシー」申告と食い違うため、実装を変えたら本文も必ず追従させる。

### 7.3 アプリ側の実装方針

| 判断 | 内容 |
|---|---|
| 課金状態の判定 | **Entitlement `pass` 一本**。商品 ID で分岐しない。商品追加や価格改定でアプリを直さずに済む |
| 状態の更新経路 | `customerInfoStream` に一本化。購入メソッド内で `hasPass` を書き換えない。失効・返金・別端末での購入も同じ経路を通る |
| キー未設定時 | `Purchases.configure` を呼ばず `.disabled` のまま動作継続。課金以外の全機能は影響を受けない |
| ペイウォール UI | RevenueCatUI ではなく自前の SwiftUI。配色を ART_PROMPTS.md の STYLE SPEC に合わせるため |
| 法務リンク | 利用規約は Apple 標準 EULA。プライバシーポリシーは要差し替え（上記5） |

---


## 8. データモデル（Codable + JSON）

SwiftData は不採用（理由はセクション15）。全状態を1つの `GameState` にまとめ、
`Codable` で JSON ファイルとして丸ごと保存・読込する。純粋な値型なのでテストが容易。

```swift
GameState                      // ← これ全体を1ファイルに保存する
  player: PlayerState
  equipment: [Equipment]
  chapters: [ChapterProgress]
  regions: [RegionState]
  bestiary: [BestiaryEntry]
  schemaVersion: Int           // 将来の形式変更に備える

PlayerState
  level: Int
  exp: Int
  ap: Int
  cumulativeSteps: Int
  lastProcessedDate: Date      // 二重計上防止
  todayCreditedSteps: Int      // 同上

Equipment
  id: String
  name: String
  slot: EquipmentSlot          // weapon / armor / accessory
  atk / def / hp: Int
  enhanceLevel: Int
  isEquipped: Bool

ChapterProgress
  chapterId: Int
  nodeIndex: Int
  isCleared: Bool

RegionState
  regionId: String
  vitality: Int                // 0...100
  isUnlocked: Bool

BestiaryEntry
  darumonId: String
  defeatedCount: Int
  firstSeenAt: Date
```

---

## 9. 画面一覧

| # | 画面 | 内容 | 優先度 |
|---|---|---|---|
| 1 | ホーム | 今日の歩数・活力・レベル・次の目的地 | 必須 |
| 2 | 章／ノード選択 | ストーリー進行 | 必須 |
| 3 | 戦闘 | ログ再生演出（3〜8秒） | 必須 |
| 4 | 装備・強化 | 意思決定の本体 | 必須 |
| 5 | 結果／報酬 | 素材獲得・図鑑登録 | 必須 |
| 6 | ペイウォール | RevenueCat | 必須 |
| 7 | 地域／復興 | やり込み層 | 高 |
| 8 | 図鑑 | 収集 | 中 |
| 9 | 設定 | 権限・デモモード・復元 | 必須 |

### 実装状況（2026-07-26）

| # | 画面 | ファイル | 状態 |
|---|---|---|---|
| 1 | ホーム | `UI/HomeView.swift` | ✅ |
| 2 | 章／ノード選択 | `UI/ChapterMapView.swift` | ✅ |
| 3 | 戦闘 | `UI/BattleView.swift` | ✅ |
| 4 | 装備・強化 | `UI/EquipmentView.swift` | ✅ |
| 5 | 結果／報酬 | `UI/ResultView.swift` | ✅ |
| 6 | ペイウォール | `Purchases/PaywallView.swift` | ✅ |
| 7 | 地域／復興 | `UI/RegionView.swift` | ✅ |
| 8 | 図鑑 | `UI/BestiaryView.swift` | ✅ |
| 9 | 設定 | `UI/EquipmentView.swift` 内 | ✅ |

戦闘演出は SwiftUI の変形のみで作っている（§15-2 / §10.1.1）。
待機はゆっくりした上下動、被弾はフラッシュとシェイク、ダメージ数値は浮上して消える。
**追加のアートを1枚も使っていない。**

---

## 10. アート要件

生成は GPT ImageGen。**品質より「シリーズとしての統一感」が課題。**

### 10.1 必要枚数（3章構成）

| 種別 | 枚数 |
|---|---|
| ダルモン（雑魚） | 12〜15 |
| ボス | 3 |
| 地域背景 | 3 |
| 主人公＋装備差分 | 1 + 数枚 |
| UIアイコン | 約20 |

### 10.1.1 戦闘のアニメーション方針（2026-07-26 検討・要判断）

「フレームアニメーションさせたい」という要望に対する評価。

**AI でコマを量産する方式は採らない。** 理由は品質ではなく**フレーム間の一貫性**。
§4 に「生成物は背景色が微妙にずれる」とあるとおり、生成物には必ずばらつきが出る。
静止画では許容できる（1枚ずつしか見ないため）が、コマ送りで再生すると輪郭が沸き立つ
「ボイリング」として一瞬で見破られる。**人間の目は静止画の不統一より動画のジッターに
桁違いに敏感。** さらにコマ間で数ピクセル位置がずれるだけで画面が揺れるため、
全コマにピクセル単位の位置合わせが要る。

量も跳ね上がる。

| | 現行計画 | フレームアニメ化 |
|---|---|---|
| 枚数 | 18枚 | **約135枚**（待機2＋攻撃3＋被弾1＋撃破2 で1体7〜8枚） |
| 後処理 | 色・余白の統一 | 上記＋全コマの位置合わせ |

**そもそもアートの方向性と衝突する。** ダルモンは sluggish / drowsy / inert な存在で、
ダラリは溶け落ちた塊、ゴロネは立つ気力もなく横たわっている。
**キビキビ動いた瞬間にコンセプトが壊れる。** ほとんど動かないことに意味がある。

#### 採用案

| 対象 | 方式 | 追加アート |
|---|---|---|
| 雑魚12〜15体 | **単一画像＋変形アニメーション**（潰し／伸ばし／回転／移動／シェーダ） | **0枚** |
| ボス3体 | 上記＋攻撃・撃破のキーフレームを2枚ずつ追加生成 | 6枚 |

変形アニメーションで表現する動き: 待機＝ゆっくり上下に揺れる／攻撃＝重そうに前へのめり込む／
被弾＝赤フラッシュ＋のけぞり＋シェイク／撃破＝傾いて崩れ落ちながらフェード。
**「重い」「気だるい」動きはこの手法が最も得意とするもの**で、アートの方向性とも一致する。

ボスだけ強化するのは、Shipaton の審査員が**動画で判断する**（§11-3）ため。
見せ場はボス戦であり、6枚の追加で最大の効果が得られる。

パーツ分割（頭・胴・腕に切って個別に動かす）は中間案として保留。表現力は上がるが、
15体分の切り分け作業が発生する。まず変形アニメーションで作り、不足を感じたら検討する。

### 10.2 統一感を担保する3つのルール

1. **スタイル記述を1つのテキストに固定する。**
   「正方形キャンバス中央、全身、単色フラット背景、太い輪郭線、彩度低めの配色、影は1段階」
   のような定型文を作り、**モンスター固有の説明だけを差し替える**。毎回書き直すと必ず崩れる。
2. **シルエットのスケールを統一する。** 生成後に全て同じキャンバスへ中央配置し、余白比率を揃える。
3. **後処理を一律にかける。** 減色（ポスタリゼーション）や軽いドット化を全アセットに通す。
   生成のばらつきを吸収でき、最も費用対効果が高い。

### 10.3 確認事項

- ImageGen 出力の商用利用可否を、**提出前に利用規約の最新版で自分で確認すること**（推測で進めない）

---

## 11. 審査対策（最重要）

**Apple のレビュアーも Shipaton の審査員も歩かない。** 対策を実装に含める。

1. **歩数ゼロでも壊れない設計** → 詳細は §11.1
2. **App Store Connect の「App Review に関する情報」に説明を記載**
   HealthKit の歩数を使うこと、デモモードでの確認手順を明記。審査通過率に直結する。
3. **提出動画をデモの本体と考える**
   審査員が触って理解するのではなく、動画で理解してもらう前提で作る。

### 11.1 チュートリアル（2026-07-27 確定・ブラウザ版で実装済み）

当初この節は「チュートリアル戦闘を1回無償提供」の一行しかなく、**実装は無かった。**
歩数ゼロで起動すると第1章のゲート（20,000歩）に届かず、討伐ボタンが押せない。
審査対策として成立していなかった。

**3戦を通して 討伐 → 素材 → 強化 → レベル を順に見せる。**
1戦では素材が3つしか出ず、強化に必要な5に届かないため、1戦では組み立てられない。

| 段 | 内容 | 何を見せるか |
|---|---|---|
| 1 | 場面（開幕） | 道を塞ぐダルモン。**履き古した靴を履く**（§1.1 の「武器は靴」） |
| 2 | ノード1 ダラリ | 戦闘の見方。澱3 |
| 3 | 場面 | 澱とは何か |
| 4 | 場面（歩く）| **2,000歩 が入り Lv2 になる。** 歩数が力になること |
| 5 | ノード2 ゴロネ | 澱3（計6）＋**目覚めの靴**を入手 |
| 6 | 場面 → 装備画面 | **澱6で強化1段（5澱）。** 段は5つまで積める |
| 7 | ノード3 ネムケ | 強化した靴で戦う。澱3 |
| 8 | 場面（締め） | ここから先は歩いた分だけ |

**戦うのは第1章のノード1・2・3そのもの。** 専用の敵を置くと、
チュートリアルで倒した相手を本編でもう一度倒すことになる。
章ゲートと活力の消費だけを外し、倒したぶんは進行として残す（抜けた人はノード4から続く）。

#### 必ず勝たせる

**歩数ゼロの Lv1 は、素のままではノード1に 61.9% でしか勝てない**（20,000回実測）。
4割の審査員が最初の戦闘で負ける状態は、この作品の第一印象をそこで決めてしまう。

二重に保証する。

- **開幕で「履き古した靴」ATK+5 を渡す。** これで3戦とも勝率100%
  （残HP 22 → 15 → 25 で、楽勝には見えない）。ATK+3 では2戦目のゴロネ（DEF10）が
  87.6% まで落ち、下の細工が表に出る
- **`BattleEngine.resolve` に `guaranteed` を渡し、倒れる一撃だけを止める**（残1で踏みとどまる）。
  削られる手応えは残る。**本編の戦闘からは決して渡さないこと**

#### レベルは歩数から導く。別枠で上げない

`GameState` は `exp` と `level` を保存せず、`cumulativeSteps` だけを真実の源にしている（§8）。
レベルだけを上げる経路を作るとこの不変条件が壊れる。**2,000歩を渡して Lv2 にする。**
活力2と道標4も同時に入り、チュートリアルを抜けた直後に「やることがある」状態で渡せる。

歩数を渡す処理は**冪等にすること**。段が進むのは進むボタンを押した時なので、
歩数を入れた直後・押す前に閉じられると、開き直すたびに歩数が入る（§3.4 と同じ話）。
入れた事実そのものを `tutorialWalked` に残す。

#### 強化の効き目は、この階では小さい

強化は `基礎値 × (1 + 0.20 × 段)` の切り捨て（§17.7）なので、
**第1章の靴 ATK+7 は1段積んでも +8 にしかならない。** 3戦目のターン数は 3.5 → 3.4。
数値の体感で「強くなった」を見せることは、いまの式では**できない**。

チュートリアルが教えるのは**仕組みと天井**（段は5つまで、最大で基礎値の2倍）に留める。
外套なら HP+50 → 60 で見えるが、外套はノード4のドロップなので3戦には入らない。

---

## 12. スケジュール

| 期間 | 内容 |
|---|---|
| 7/25〜7/31 | ✅ Xcode プロジェクト作成 / ✅ HealthKit 疎通確認 / ✅ Identifier 登録（HealthKit 有効）/ ✅ App Store Connect アプリ枠作成 / ✅ Git 公開リポジトリ / RevenueCat 登録 / Devpost 登録 / アートスタイル確定（ダルモン3体で試作） |
| 8/1〜8/14 | コアループ実装（歩数→EXP/AP→自動戦闘→報酬）。**公開はしない** |
| 8/15〜8/31 | ストーリー3章・地域・ボス・装備システム |
| 9/1〜9/10 | RevenueCat 課金とペイウォール・やり込み層・TestFlight |
| 9/10〜9/15 | **審査提出**（2回リジェクトされる前提で余裕を取る） |
| 9/15〜9/30 | リリース確認・Build in Public 発信・提出動画と Devpost 記入 |

---

## 13. 技術構成

| 項目 | 選択 | 状態 |
|---|---|---|
| ツールチェイン | Xcode 26.3 / Swift 6.2 | 確定 |
| **Swift 言語モード** | **5.0**（`SWIFT_VERSION = 5.0`） | 確定 |
| UI | SwiftUI（`@Observable` を使用） | 確定 |
| 最低 OS | iOS 17.0 | 確定 |
| 対応デバイス | **iPhone のみ**（`TARGETED_DEVICE_FAMILY = 1`） | 確定 |
| 歩数取得 | HealthKit（`HKStatisticsCollectionQuery`、日別バケット） | 確定・**検証済み** |
| 課金 | RevenueCat SDK 5.81.2（`purchases-ios-spm`） | 確定・**実装済み**（購入の実地検証のみ未了 → §7.4） |
| サーバー | なし（オンデバイス完結） | 確定 |
| プロジェクト形式 | Xcode 16+ のファイルシステム同期グループ | 確定 |
| 永続化 | **Codable + JSON**（SwiftData は不採用 → §15-1） | 確定・**未実装** |

**言語モードを 6 ではなく 5 にした理由**: Swift 6 の厳格な並行性チェックは、HealthKit のような
Sendable 対応が不完全なフレームワークを扱うと大量のエラーを生む。2ヶ月の締切で
コンパイラと戦う時間はないため、意図的に 5 を選択している。後から 6 へ上げることは可能。

### 実装済み

- `WalkUP/Health/StepProvider.swift` — 歩数供給元のプロトコル（日別粒度に統一）
- `WalkUP/Health/HealthKitStepProvider.swift` — 本番実装
- `WalkUP/Health/MockStepProvider.swift` — 決定論的な擬似データ（開発／審査／デモ兼用）
- `WalkUP/StepDashboardModel.swift` — 疎通確認用モデル（→ 活力変換層に発展させる）
- `WalkUP/ContentView.swift` — 疎通確認画面（→ ホーム画面に差し替える）
- `WalkUP/AppSecrets.swift` — 秘匿値へのアクセス層
- `Config.xcconfig` / `Secrets.xcconfig.example` / `Info.plist` — 秘匿値の注入経路
- `WalkUP/Purchases/PurchaseIdentifiers.swift` — Entitlement / Offering / Product ID の集約
- `WalkUP/Purchases/PurchaseManager.swift` — RevenueCat との唯一の接点（初期化・取得・購入・復元）
- `WalkUP/Purchases/PaywallView.swift` — 活力パスのペイウォール（画面 #6）

- `WalkUP/Assets.xcassets/AppIcon.appiconset/` — **暫定**アプリアイコン（差し替え前提）

依存: `purchases-ios-spm` 5.81.2（`upToNextMajorVersion`）。`Package.resolved` はコミット対象。

検証済みの範囲: シミュレータ・実機の両方でビルド・起動、SDK 初期化、RevenueCat
サーバーへの到達、ペイウォールのライト/ダーク表示。**購入・復元は未検証**（§7.4）。

### App Store アップロード時に必要だった構成（再発防止）

初回アップロードが検証で3件弾かれた。いずれも構成の不足で、実装とは無関係。

| 項目 | 注意点 |
|---|---|
| アプリアイコン | 1024px を1枚。**透過不可**（アルファチャンネルを持たせない） |
| `NSHealthUpdateUsageDescription` | 書き込まなくても、**HealthKit entitlement を持つだけで必須** |
| `CFBundleIconName` | actool は `CFBundleIcons` の入れ子にしか出力しない。**トップレベルに明示が必要**（`Info.plist`） |

### 秘匿値の扱い

```
Secrets.xcconfig (.gitignore 済み)
  → ビルド設定 REVENUECAT_API_KEY
  → Info.plist の RevenueCatAPIKey
  → AppSecrets.revenueCatAPIKey
```

`Config.xcconfig` が既定値を空で定義したうえで `#include?` により `Secrets.xcconfig` を
任意読み込みするため、**クローン直後（キー未設定）でもビルドが通る**。
未設定時 `AppSecrets.revenueCatAPIKey` は `nil` を返すので、課金機能を無効化して動作を継続できる。

セットアップ手順:

```
cp Secrets.xcconfig.example Secrets.xcconfig
# RevenueCat ダッシュボード → Project settings → API keys の
# Public app-specific API key (appl_ で始まる) を記入する
```

なお `INFOPLIST_KEY_<任意名>` は任意キーには使えないため、`Info.plist` を明示指定している。
`GENERATE_INFOPLIST_FILE = YES` と併用しており、表示名や HealthKit 説明文は従来どおり
ビルド設定から自動生成される（検証済み）。

**検証済み**（2026-07-25、iPhone 17 Pro / iOS 26.2 シミュレータ）

- HealthKit entitlement と用途説明文が許可ダイアログに正しく表示される
- `HKStatisticsCollectionQuery` が日別バケットを7日分返す
- 歩数ゼロでもクラッシュせず表示される（＝ App Review 時の挙動）
- デモモードが休養日を含む現実的なデータを生成する
- ホーム画面の表示名「Walk UP!」が切り詰められない

---

## 14. 未決事項

| # | 項目 | 備考 |
|---|---|---|
| 1 | 章タイトル・ストーリー本文 | 8月前半までに執筆 |
| 2 | ダルモンのビジュアル方向性 | 7月中に3体試作して決定 |
| 3 | 価格の最終決定 | 日本以外の地域設定も含めて |
| 4 | 章ゲートの最終調整 | 実データで歩数分布を見てから |
**確定済み**

- ~~アプリ名~~ → ストア名 **Walk UP! 万歩計RPG** / 表示名 **Walk UP!**（2026-07-25 確定。どちらも後から変更可能）
- ~~Bundle ID~~ → **`com.akaoonaga.walkup`**（2026-07-25 確定。**変更不可**）

---
## 15. 技術判断（2026-07-25 確定）

| # | 項目 | 決定 |
|---|---|---|
| 1 | 永続化 | **Codable + JSON ファイル**（SwiftData は不採用） |
| 2 | 戦闘演出 | **SwiftUI のみ**（SpriteKit は使わない） |
| 3 | 言語対応 | **完全二言語（日本語 / 英語）**。物語本文も含む |
| 4 | ローカル通知 | **入れる**。1日1通。**第1章クリア後**に許可を求める |
| 5 | HealthKit バックグラウンド更新 | **不要**（アプリ起動時のみ取得） |
| 6 | ウィジェット / Live Activity | **スコープ外** |
| 7 | テスト | **歩数変換と戦闘計算のみユニットテスト**。UI テストは書かない |
| 8 | アナリティクス | **入れない** |
| 9 | ダークモード対応 | **アートを背景透過で生成し、背景はアプリ側で敷く**（下記） |

### 9. ダークモード対応（2026-07-26 確定：案B・背景透過）

ダルモンのアートは **背景 `#E8E4DC` で焼き込み**（ART_PROMPTS.md の STYLE SPEC）。
このためダークモードでは、暗い画面の中にモンスターの明るい四角が浮く。

選択肢は2つ。

| 案 | 内容 | 代償 |
|---|---|---|
| A | アプリ全体をライト固定（`.preferredColorScheme(.light)`） | 夜に遊ぶゲームで眩しい。本作は「夜1セッション3分」が前提（§2）なので相性が悪い |
| B | アートを背景透過で書き出し、背景はアプリ側で敷く | 後処理（ART_PROMPTS.md §4）に背景除去の工程が増える |

**案B を採用した。** ライト固定は「夜1セッション3分」（§2）と相性が悪く、
また透過なら**地域背景をモンスターの後ろに敷ける**のでゲームとしての見栄えも上がる。
ART_PROMPTS.md の STYLE SPEC と後処理（§4）を透過前提に改訂済み。

**量産前に決めておく必要があった判断。** 12〜15体を焼き込み背景で作った後に
倒していたら、全部作り直しだった。

なお実装上の教訓として、**背景色を固定するなら文字色も必ずセットで面倒を見ること**。
ペイウォールで背景だけ固定し文字を `.primary` / `.secondary` のままにしたところ、
ダークモードで「明るい背景に白文字」になり読めなくなった（コミット `ac0ce1a`）。
現在は背景側をモード追従にして解決している。


### 9.1 モード追従をやめた（2026-07-27 確定・上の判断を上書き）

**OS のライト／ダークには追従しない。紙の色（#E8E4DC）に固定する。**

上では「ライト固定は夜に眩しい」として案B（背景透過＋モード追従）を採ったが、
実際に全画面を作り込む段になって、追従そのものが問題だと分かった。

| 追従させると | 起きること |
|---|---|
| 同じ画面が端末の設定で二つの顔を持つ | 作品としての色が決まらない |
| どちらでも破綻しないことを毎回確認する必要 | そのぶん一つ一つの詰めが甘くなる |
| `.primary` / `.secondary` が反転する | 背景を固定した画面で文字が消える（2度やらかした） |

固定先を紙にしたのは、**ダルモンの立ち絵がこの色の上で描かれている**ため
（`ART_PROMPTS.md` §2 の STYLE SPEC）。暗い側に寄せると、絵の前提と画面が食い違う。
戦闘画面だけは地域アートを敷いて暗いままで、そこは画像なのでモードとは無関係。

実装は3か所で固定している。**1か所でも外すと元に戻る。**

| 場所 | 内容 |
|---|---|
| `Theme` | 8色すべて確定値。`text` / `textSoft` を持ち、`.primary` / `.secondary` は使わない |
| `WalkUPApp` | `.preferredColorScheme(.light)` と `.tint(Theme.accent)` |
| `project.pbxproj` | `INFOPLIST_KEY_UIUserInterfaceStyle = Light` |

ルートだけ、あるいは Info.plist だけでは、シートやアラートなど **OS 側が描く部分が反転する。**

### 1. 永続化を JSON にした理由

データは単一ユーザーの数十KB程度で、クエリもリレーションも不要。SwiftData はマイグレーション周りで
詰まると原因究明に時間を取られ、2ヶ月の締切では致命傷になりうる。JSON なら状態を丸ごと
保存・読込するだけで、純粋関数として書けるためテストも容易。将来の移行も可能。

### 2. 戦闘演出に SpriteKit を使わない理由

§4.1 が求めているのは「3〜8秒のログ再生」であり、勝敗は戦闘開始前に確定している。
**リアルタイム入力も物理も衝突判定もスクロールする世界も無い。**
必要なのは、2体を並べて HP バーを減らし、数値を出し、被弾で光らせ・揺らすことだけ。

これは iOS 17 の SwiftUI で全部できる（最低 OS が 17.0 なので全機能が使える）。

| やりたいこと | 手段 |
|---|---|
| 攻撃モーションの連続演出 | `.keyframeAnimator` / `.phaseAnimator` |
| ダメージ数値・HP のカウント | `.contentTransition(.numericText())` |
| 被弾フラッシュ・シェイク | `.offset` / `.scaleEffect` + `withAnimation` |
| 派手なエフェクト | **Metal シェーダ**（`.colorEffect` / `.distortionEffect` / `.layerEffect`） |

**「SwiftUI だと地味になる」は iOS 17 以降は成り立たない。** シェーダが使えるため。

SpriteKit を入れた場合のコスト:

1. レンダリング系が2つになる。`SpriteView` を挟むと座標系・ライフサイクル・
   `@Observable` との状態同期を手で管理することになる
2. HP バー・ラベル・ボタンを SpriteKit 側で作り直すことになり、UI が退化する
3. **アセット前提が合わない。** ART_PROMPTS.md の出力は 1:1 の静止イラストで、
   スプライトシートもフレームアニメも作る計画が無い。SpriteKit の主戦場
   （テクスチャアトラス・パーティクル・物理）を1つも使わない

SpriteKit が正解になるのは、フレームアニメを大量に回す／多数の敵が同時に動く／
物理や当たり判定が要る／世界をスクロールする場合。**本作はどれにも当てはまらない。**

**フレームアニメーション化しても、この判断は変わらない。** 画面上に2体、3〜8秒であれば
`TimelineView` や `.phaseAnimator` で画像を差し替えるだけで足りる。
SpriteKit が効いてくるのは多数のエンティティが同時に動く場合であり、本作は該当しない。

判断が崩れるとすれば、戦闘以外に多数の敵が同時に動く画面を作ることにした場合。
現時点でそのような画面は §9 の一覧に無い。

### 3. 完全二言語化の方針

**全てのユーザー向け文言を最初から String Catalog（`.xcstrings`）に通すこと。**
後から retrofit すると、散らばったハードコード文字列を拾い直す作業が翻訳本体より高くつく。
現時点で `ContentView.swift` / `StepProvider.swift` にハードコードされた日本語も対象。

翻訳は LLM を使う。ただし**固有名詞の対訳表を先に固定する**こと。放置すると同じ語が章ごとに
違う訳語になる。

| 日本語 | English | 備考 |
|---|---|---|
| ダルモン | **Dullmon** | 「怠い＋モン」と "dull + mon" が同じ構造。訳ではなく等価物なので品質が落ちない |
| ダラリ | Darari | 固有名はローマ字表記で統一 |
| ネムケ | Nemuke | 同上 |
| ゴロネ | Gorone | 同上 |
| 活力 | Vigor | ゲーム内資源（AP） |
| 活力パス | Vigor Pass | サブスク表示名 |
| 復興 | Restoration | やり込み層の軸 |

**App Store Connect 側の英語対応も必要。** 現状サブスクリプションの表示名・説明は日本語のみ。
ストア掲載情報（アプリ説明・スクリーンショット）も英語版が要る。

### 4. 通知を第1章クリア後にする理由

初回起動時に許可を求めるとオンボーディングが重くなり、初日離脱が増える。
第1章クリアは「面白い」と判断した直後なので、許可率が最も高くなる地点。

### 7. テスト対象を絞る理由

歩数の二重計上（日付変更・端末時刻の巻き戻し）と戦闘バランスは、目視テストでは必ず漏れる。
逆に UI テストは維持コストが高く、締切に合わない。

### 7.2 なぜこの配置か

- サブスクは月次の歩行サイクルと噛み合い、RevenueCat の設計にそのまま乗る
- スキンはバランス非干渉なので安全
- 遡り回収は「歩いた事実」を変えないため、アプリの前提を否定しない

### 7.3 コスト見積もり

現時点で外部APIへの依存は無い（AI生成テキスト・TTS は MVP から除外）。
サーバー不要・オンデバイス完結のため、ランニングコストは実質ゼロ。

---

## 16. コアループ実装前の仕様ギャップ（2026-07-26 洗い出し）

実装着手にあたり DESIGN.md を再点検した結果、**前半は着手可能、後半は数値未定で着手不能**と判明した。
「とりあえず書き始めて後で埋める」をやると、マジックナンバーがコードに散り、
バランス調整のたびに全体を触ることになる。マスターデータは先に確定させる。

### 16.1 いま実装できる範囲（仕様は完備）

| 対象 | 根拠 |
|---|---|
| 歩数 → EXP / AP 変換 | §3.1（1歩=1EXP、1,000歩=1AP、日上限30,000） |
| レベル曲線・基礎ステータス | §3.2 / §3.3（式が確定） |
| 二重計上の防止 | §3.4（アルゴリズムまで記述済み） |
| `GameState` の定義と JSON 永続化 | §8 / §15-1 |
| 上記のユニットテスト | §15-7 で対象として名指し |

**この範囲だけで「歩けばレベルが上がる」まで動く。** ここを先に完成させる。

### 16.2 決めないと戦闘が書けない数値

いずれも DESIGN.md に記述が無い。

| # | 項目 | なぜ要るか |
|---|---|---|
| 1 | **討伐1回のAP消費量** | §3.1 は「AP → 討伐回数」と書くのみ。1回あたりの消費が無いと AP が設計値にならない |
| 2 | **ダルモン12〜15体のステータス** | §4.4 にあるのは第1章ボスのみ。雑魚の HP/ATK/DEF が無い |
| 3 | **ノード構成（どのノードに何が出るか）** | §5.1 は「8〜10ノード」とあるだけ |
| 4 | **素材（インベントリ）** | §2・§6 が前提にしているが、**`GameState` に該当フィールドが無い**（§8 の設計漏れ） |
| 5 | **装備カタログ** | `Equipment` 型はあるが、実在する装備の一覧と入手条件が無い |
| 6 | **強化の費用と効果** | `enhanceLevel` はあるが式が無い |
| 7 | **ドロップ量**（勝利時／敗北時） | §4.3 の「一部は持ち帰れる」の割合が未定義 |
| 8 | **活気ゲージの上昇量** | `vitality: 0...100` の増え方が無い |
| 9 | **活力パスの効果倍率** | **課金の価値そのもの。**§7 は「増える」としか書いていない |

**#4 は設計漏れ**であり、単なる未決ではない。素材が保存されないと強化も図鑑も成立しないため、
`GameState` に `materials: [String: Int]` 相当の追加が要る。

**#9 は最優先。** 効果量が決まらないとペイウォールの訴求文（実装済み）が
実態と食い違ったままになり、審査で「機能の説明と実際が異なる」と見なされうる。

### 16.3 進め方

1. §16.1 の範囲を実装＋テスト（仕様完備・依存なし）
2. 並行して §16.2 の9項目を確定させる
3. 確定後に戦闘・報酬・強化へ進む

§12 のスケジュールでは 8/1〜8/14 がコアループ実装に充てられている。
1 は前倒しで進めてよいが、2 を飛ばして 3 に入らないこと。

---

## 17. マスターデータ叩き台（2026-07-26・要レビュー）

§16.2 で未定だった9項目を確定させるための叩き台。**前提を2つ置いている。**

| 前提 | 値 |
|---|---|
| 想定歩数 | **1日 4,000歩** |
| 活力パスの効果 | **素材 1.5倍 / 復興 1.5倍** |

1日4,000歩 = **4 AP/日**、4,000 EXP/日。

### 17.0 章ゲートは変更しない

§5.1 のゲート（20,000 / 60,000 / 120,000）は据え置く。
ゲートは歩数＝累計EXPで切られているため、**1日あたりの歩数が変わっても到達レベルは変わらない**。
伸びるのは日数だけ。

| 章 | ゲート | 到達Lv | 到達日数(6,000歩/日) | 到達日数(**4,000歩/日**) |
|---|---|---|---|---|
| 第1章 | 20,000 | **Lv6** | 約3日 | **5日** |
| 第2章 | 60,000 | **Lv10** | 約9日 | **15日** |
| 第3章 | 120,000 | **Lv15** | 約18日 | **30日** |

Lv = `floor(sqrt(累計EXP / 500))`。

**§3.2 と §5.1 の「到達目安」列は 1日6,000〜6,700歩を前提にした数字**であり、
4,000歩/日では上表の右列になる。表そのものは残すが、この差を認識しておくこと。

到達Lvが 6 / 10 / 15 となるため、**§4.4 の第1章ボス（Lv6想定・HP300/ATK25/DEF10）は
そのまま有効**。17.3 は同じ設計原則で第2・3章のボスを追加したもの。

なお審査員への対応は §11-1 のデモモードが担うため、日数が伸びても審査には影響しない。

### 17.1 AP 消費量（#1）

| 行動 | 消費 |
|---|---|
| 通常討伐 | **1 AP** |
| ボス戦 | **3 AP** |

4 AP/日なので、通常日は「雑魚4回」または「ボス1回＋雑魚1回」。§2 の「1セッション3分」に収まる。
ボスは1日分の AP をほぼ使い切るため、挑む判断に重みが出る。

1章あたりの消費は 7 + 3 = **10 AP**（2.5日分）。一方で章ゲートの到達には5〜15日かかるため、
**AP は必ず余る。** 余剰は §6 のやり込み層（周回・活気ゲージ）が受け止める設計。

### 17.2 ダルモン（雑魚）のステータス（#2）

個別に手で決めず、**式から出す**。想定Lvのプレイヤーに対し
「4ターンで倒せる／HPの25%を削られる」を基準にした。

```
雑魚HP  = 4 × (プレイヤーATK − 雑魚DEF / 2)
雑魚ATK = プレイヤーHP × 0.0625 + プレイヤーDEF / 2
```

基準Lvは各章の中間地点を採る。3つの役割で変化をつけ、
シルエットの差（ART_PROMPTS.md §3）と対応させる。

| 章 | 基準Lv | 役割 | HP | ATK | DEF |
|---|---|---|---|---|---|
| 1 | 4 | 標準（ダラリ型） | 80 | 12 | 4 |
| 1 | 4 | 硬い（ゴロネ型） | 68 | 13 | 10 |
| 1 | 4 | 手数（ネムケ型） | 67 | 15 | 2 |
| 2 | 8 | 標準 | 124 | 19 | 6 |
| 2 | 8 | 硬い | 104 | 20 | 16 |
| 2 | 8 | 手数 | 104 | 23 | 3 |
| 3 | 12 | 標準 | 168 | 25 | 8 |
| 3 | 12 | 硬い | 144 | 26 | 20 |
| 3 | 12 | 手数 | 141 | 29 | 4 |

12〜15体は、この9パターンに見た目と名前の差分を割り当てて作る。
**式を持っておくこと自体が重要**で、テンポが合わなければ係数（4ターン／25%）だけを触れば
全体が一括で調整される。

### 17.3 ボス（#2 続き）

§4.4 の設計原則「**装備なしでは負け、装備ありで辛勝**」を全章で維持する。
第1章は §4.4 の既存値をそのまま採用。

**HP は 20,000回のシミュレーションで決めた実測値（§17.12）。** 期待値だけで置いた
当初案（300 / 500 / 720）は、実測すると装備を揃えた時点で勝率100%になり破綻していた。

| 章 | 想定Lv | HP | ATK | DEF |
|---|---|---|---|---|
| 1 | 6 | **370** | 25 | 10 |
| 2 | 10 | **570** | 38 | 16 |
| 3 | 15 | **890** | 52 | 22 |

実測（乱数 ±20%・各20,000回）:

| 章 | 装備なし | 装備あり・強化なし | 強化+1 |
|---|---|---|---|
| 1 | **0.0%** | **87.0%**（11.2T・残HP7%） | 99.8% |
| 2 | **0.0%** | **81.9%**（11.3T・残HP6%） | 100% |
| 3 | **0.0%** | **79.1%**（12.3T・残HP6%） | 100% |

**装備なしでは絶対に勝てず、揃えても2割は負け、強化すれば確実に勝てる。**
§4.4 の原則を保ったまま、§17.7 の強化に役割を与えている。
30ターン上限（§4.2）にも余裕で収まる。

### 17.4 ノード構成（#3）

各章 8ノード。**7つの通常討伐＋最後にボス。**

| ノード | 内容 | AP |
|---|---|---|
| 1, 3, 5, 7 | 標準／手数の雑魚 | 各1 |
| 2, 4, 6 | 硬い雑魚＋**装備を確定ドロップ** | 各1 |
| 8 | ボス | 3 |

**装備は素材から作らせず、ノード2/4/6で確定入手させる。** 素材集めを前提にすると、
運が悪いプレイヤーが §4.4 の「装備ありで辛勝」に到達できない。素材は強化専用にする。

### 17.5 素材とインベントリ（#4・**8章の設計漏れ**）

`GameState` に保存先が無かった。以下を追加する。

```swift
GameState
  materials: [String: Int]      // ← 追加。素材ID → 所持数
```

MVP は2種類のみ。

| 素材ID | 名称 | 入手 | 用途 |
|---|---|---|---|
| `dregs` | 怠惰の澱 | 全ダルモン | 装備強化 |
| `core` | 怠惰の核 | ボスのみ | 章をまたぐ強化の解放 |

### 17.6 装備カタログ（#5）

3スロット × 3章 = 9個。**各章の合計値が 17.3 の検証前提と一致している。**
第1章の合計は §4.4 の「装備あり Lv6 = HP160 / ATK38 / DEF22」と厳密に一致する。

| 章 | 武器 | 防具 | 装飾 | 合計 |
|---|---|---|---|---|
| 1 | ATK+7 | HP+50 DEF+3 | ATK+3 DEF+2 | **HP+50 ATK+10 DEF+5** |
| 2 | ATK+13 | HP+90 DEF+6 | ATK+5 DEF+3 | **HP+90 ATK+18 DEF+9** |
| 3 | ATK+20 | HP+140 DEF+9 | ATK+8 DEF+5 | **HP+140 ATK+28 DEF+14** |

### 17.7 強化（#6）

```
強化後のステータス = 基礎値 × (1 + 0.20 × enhanceLevel)
必要な澱          = 5 × (enhanceLevel + 1)²
```

`enhanceLevel` は 0〜5。最大で基礎値の2倍。
0→5 の総コスト = 5+20+45+80+125 = **275 澱**。

### 17.8 ドロップ量（#7）

| 状況 | 澱 | 核 |
|---|---|---|
| 雑魚に勝利 | 3 | — |
| ボスに勝利 | 10 | 1 |
| **敗北**（§4.3「一部は持ち帰れる」） | 勝利時の 1/3・切り上げ | 0 |

**活力パス適用時は澱が 1.5倍（切り上げ）。** 雑魚3→5、ボス10→15。

1日4 AP＝雑魚4回＝12澱。装備1つを最大強化（275澱）に約23日。
パスありなら約15日。**これが課金の体感価値になる。**

### 17.9 活気ゲージ（#8）

`RegionState.vitality` は 0〜100。

| 行動 | 上昇 | パス適用時 |
|---|---|---|
| 雑魚討伐 | +2 | +3 |
| ボス討伐 | +10 | +15 |

100 到達に雑魚50回 = 50 AP = **12.5日**（パスありで8.3日）。

### 17.10 活力パスの効果（#9・確定）

| 対象 | 倍率 |
|---|---|
| 素材ドロップ | **1.5倍**（切り上げ） |
| 活気ゲージ上昇 | **1.5倍** |
| EXP / AP / 歩数 | **効果なし**（§2 の絶対ルール2「歩数を買わせない」） |

実装済みペイウォールの訴求文（「素材の獲得量が増える」「復興が早く進む」
「歩数は買えません」）と一致している。**倍率を変える場合は訴求文も必ず追従させること。**

### 17.11 検証結果（2026-07-26・シミュレーション実施済み）

§17.11 で未検証としていた項目を、戦闘エンジンの実装後に 20,000回まわして実測した。

| 項目 | 結果 |
|---|---|
| 雑魚のターン数 | **3.3〜4.0ターン**。§17.2 の想定（4ターン）どおり ✅ |
| 雑魚の被ダメージ | **21〜27%**。§17.2 の想定（25%）どおり ✅ |
| 雑魚の勝率 | 全9パターンで **100%**。周回対象として妥当 ✅ |
| ボス（装備なし） | 全章 **0.0%**。§4.4 の原則どおり ✅ |
| ボス（装備あり） | 当初案では **100%** で「辛勝」になっていなかった ❌ → §17.12 で修正 |
| セッション時間 | 雑魚4回で合計15ターン前後。3分に収まる ✅ |

**雑魚の設計式（§17.2）は実測でそのまま通用した。** 修正が必要だったのはボスのみ。

残る懸念: 第3章到達に30日かかる点（4,000歩/日）。§5.2 が想定する速度より遅く離脱リスクがある。
ゲートを下げずに解決するなら、やり込み層（§6）を早期に開放する手がある。

### 17.12 シミュレーションで判明した2つの問題（2026-07-26）

紙の上では気付けず、実測して初めて分かった。**期待値だけで戦闘を設計してはいけない。**

#### 問題1: ±10% の乱数では「辛勝」が原理的に作れない

§4.2 の乱数幅は当初 ±10%（0.9〜1.1）だった。ところが10ターン以上かかる戦闘では
**振れが平均に収束し、勝敗が実質的に決定論になる。**
「10ターンで倒す vs 11.4ターン耐える」と紙に書いた時点で、勝敗は確定していた。

実測した勝率の崖（第1章 Lv6・装備あり）:

| ボスHP | ±10%（当初） | ±20%（採用） | ±35% |
|---|---|---|---|
| 380 | 97.7% | 77.6% | 65.0% |
| 400 | 29.1% | 37.8% | 41.3% |
| **落差** | **68pt** | **40pt** | **24pt** |

±10% では HP を20動かすだけで勝率が68ポイント飛び、**調整そのものが成立しない。**
±20% を採用して §4.2 を改訂した。±35% 以上は運の比重が大きくなりすぎるため採らない。

#### 問題2: 装備を揃えるだけで確定勝利だと、強化に存在意義が無い

当初のボスHP（300 / 500 / 720）では、装備を揃えた時点で勝率100%だった。
§4.4 の「装備なしでは負け、装備ありで勝つ」は満たしていたが、
**それだけだと §17.7 の強化システムに役割が無い。**
素材を集める理由が消え、連鎖して**活力パス（素材1.5倍）の価値まで無意味になる。**
課金の中身が空洞化するため、これは単なるバランスの問題ではない。

ボスHPを上げ、「装備を揃えて8割、強化して確実」という構造にした。

#### 再現方法

`WalkUPTests/BalanceSimulationTests.swift` が回帰テストとして常時走る。
数値を変えたらテストが落ちるので、崩れたことに気付ける。

---

## 18. 道標システム（2026-07-26 提案・要レビュー）

### 18.1 解決したい問題

§17.11 で残した懸念そのもの。4,000歩/日では第3章到達に30日かかり、
**章ノードの間に数日単位の空白が生まれる。** その間、歩いても目に見える変化が無い。

500歩（約5分・400m）ごとに小さな手応えを返す層を、AP の下に追加する。

### 18.2 設計上の制約（守るべき2点）

**1. 素材経済に触れないこと。**
500歩ごとに澱を配ると1日8〜12個増え、雑魚討伐（12澱/日）と同等になる。
§17.7 の「最大強化まで23日／パスあり15日」が崩れ、**活力パスの価値が薄まる。**
課金の中身に直撃するため、道標から澱は出さない。

**2. §2 の「1セッション3分」を壊さないこと。**
8個の宝箱を1つずつ開けさせると作業になる。**一括開封**にする。

### 18.3 仕様

| 項目 | 値 |
|---|---|
| 付与間隔 | **500歩ごとに1つ** |
| 日次上限 | **12個**（6,000歩相当） |
| 開封 | アプリ起動時に**一括**。タップ1回、演出3〜5秒 |
| EXP / AP への影響 | **なし**（従来どおり30,000歩まで加算される） |

日次上限を設けないと、30,000歩の日に60個出て経済が壊れる。

### 18.4 中身の抽選表

| 出目 | 確率 | 内容 |
|---|---|---|
| 世界の記述 | 65% | 物語の断片。地域・図鑑の記述が埋まる |
| 目撃情報 | 10% | 未発見ダルモンが図鑑に「影」として登録される。討伐で確定 |
| 核のかけら | 25% | 10個で「怠惰の核」1つ。ボス以外で核を得る唯一の経路 |

#### 携行品を廃止した（2026-07-27）

当初は 20% で「戦闘前に使える回復／一時強化」を置いていたが、**この戦闘設計では回復が成立しない。**
§4.1 のとおり戦闘は入る前に勝敗まで確定し、毎回 HP 全快で始まる。
戦闘をまたいで HP が持ち越されないので、**回復量をいくつに決めても何も起きない。**

一時強化に読み替えれば機能はするが、道具を選ぶ・数える・切らす、という層が1つ増える。
§2 の「1セッション3分」に対して割に合わないため、概念ごと落とした。
20% は「世界の記述」に回し、図鑑（目撃）と経済（かけら）の比だけを入れ替えている。

#### 「世界の記述」は1日2本まで（2026-07-27）

**枯渇は本数ではなく出す速度の問題だった。** 65% × 12個 = 約8本/日 では、
何本書き足しても数日で尽きる。実測でも第1章ぶんの12本が2日で消えていた。

§6.3 が既に「読み物を一度に出すと読まれない」と書いているのと同じ話で、
12本まとめて渡しても記憶に残らない。**日次上限を2本にする。**
溢れたぶんは目撃・かけらへ回る。

あわせて本数を 22 → **44本**（第1章20・第2章14・第3章10）に増やした。
第1章は 20,000→60,000歩 で約10日なので、2本/日 × 10日 = 20本でちょうど埋まる。

**記述と場面（§6.1）で役割を分ける。** 場面が筋を担い、記述は街の細部に徹する。
記述に事件を書かない。書くと、場面を読まなくても話が分かってしまう。

#### 引き切った時の逃がし先

記述22本を読み終える、あるいは全個体を目撃し終えると、その出目は行き先を失う。
**ここでかけらへ直行させないこと。** 記述は数日で尽きるため、
65% がそのままかけらに化けて核が1日おきに手に入るようになる。
逃がし先は**もう一方の読み物側**（記述↔目撃）にし、両方尽きた時だけかけらにする。

**「世界の記述」が本命。** アートもデータモデルも不要で、テキストだけで作れる。
そして §1 のコンセプト——世界は怠惰に堕ち、歩数だけがそれを打ち破る資源——と一致する。
**歩いた分だけ世界の描写が戻ってくる。**
章が進まない日でも世界は取り戻されていくため、30日の空白が「進めない期間」ではなく
「世界を掘る期間」になる。

核のかけらは 25% × 12個 = 3個/日 で、**核1つに約3.3日**。
ボスからは確実に1個手に入るため、これは補助経路。

**核には現在まったく使い道が無い**（§17.5 の「章をまたぐ強化の解放」が未実装）。
強化が消費するのは澱だけなので、いま流入を増やしても壊れる物は無いが、
用途を作る時はこの 3.3日 を前提に設計すること。

### 18.5 実装方針

既存の仕組みで足りる。**HealthKit の粒度は日別のまま変更不要。**

```
当日の獲得数 = min(floor(当日歩数 / 500), 12)
付与すべき数 = 当日の獲得数 − PlayerState.milestoneCreditedToday
```

§3.4 の二重計上防止とまったく同じ構造。過去日の遡り処理も同じ経路に乗る。

`GameState` への追加:

```swift
PlayerState
  milestoneCreditedToday: Int   // ← 追加。当日の付与済み道標数

GameState
  unlockedLore: [String]        // ← 追加。解放済みの世界の記述
```

`BestiaryEntry` に「目撃済みだが未討伐」を表す状態が要る（現状は `defeatedCount` のみ）。

### 18.6 未決

- ~~「世界の記述」の総数~~ → 44本＋日次上限2本で解決（2026-07-27）
- ~~「怠惰の核」の用途~~ → §6.2 の「市」で澱・かけらに替えられる（2026-07-27）
- 活力パスを道標にも効かせるか。**効かせない方が良い**と考える。
  §18.2 の制約1と同じ理由で、経済と課金価値の分離を保つため
- スケジュールへの影響。§12 の 8/1〜8/14（コアループ）に上乗せとなる
