# 引き継ぎメモ（2026-07-27）

新しいセッションはこのファイルから読み始めること。
詳細は `DESIGN.md`（仕様）・`ART_PROMPTS.md`（アート）・`web/README.md`（画面デザイン）にある。
**このファイルは現在地と落とし穴だけを書く。仕様は書かない**（二重管理になるため）。

## この作品は何か

RevenueCat Shipaton 2026 提出作品の万歩計RPG「Walk UP! 万歩計RPG」。
歩数だけが怠惰（ダルモン）を打ち破る資源、という一文が設計原理（`DESIGN.md` §1）。
**提出期限は 2026/9/30、App Store 公開は 8/1 以降でないと失格**（§0）。

## 現在地

| 領域 | 状態 |
|---|---|
| コアループ（歩数→EXP/AP→戦闘→報酬→強化） | ✅ 動作。実機確認済み |
| 画面 | ✅ 全9画面。§9 の #7 #8（地域・図鑑）も実装済み |
| ユニットテスト | ✅ 39件（`WalkUPTests/`） |
| マスターデータ | ✅ 確定（§17）。20,000回シミュレーションで検証済み |
| アート（本編必須） | ✅ 雑魚12体・主人公・地域背景3枚・ボス3体 |
| アート（追加・2026-07-27） | ✅ 復興後背景3枚・装備9点・素材4点・小物4点・章の扉3枚・タイトル絵・主人公の立ち姿 |
| 課金（RevenueCat） | ✅ 実装完了。**購入の実地検証のみ未了**（§7.4） |
| TestFlight | ✅ 0.1.0 / build 1 アップロード済み |
| プライバシーポリシー | ✅ 公開済み（GitHub Pages） |
| **画面デザインの見本（`web/`）** | ✅ 全画面。**ここが意匠の正**。詳細は `web/README.md` |
| チュートリアル（§11.1） | ✅ ブラウザ版のみ。3戦・必勝・素材→強化→レベル。**Swift 未実装** |
| **見本 → Swift の移植** | ❌ **未着手。追加アセットもコードから参照されていない** |

## 画面デザインは `web/` で決めてから Swift に移す

`web/` にブラウザで遊べる実装がある（`open web/index.html`）。
**これは飾りではなく、画面デザインの決定版。** SwiftUI を書く前に必ず開くこと。

なぜこうしているか。**SwiftUI に素直に書くと健康アプリ・習慣化アプリの顔になる。**
角丸カードの縦積み、行で組んだ一覧、`.navigationTitle`、SF Symbols——
標準部品が最短距離で作れる形が、そのまま「記録アプリ」の構造だから。
ブラウザで速く試して構造を決め、決まった物だけ Swift へ移す。

守っている規則と対応表は `web/README.md`。**要点だけ挙げると：**

- 画面を2種類に分ける。**世界**（タイトル・導入・扉・ホーム・討伐・戦闘・結果・完結）は
  アートが地で UI が上に浮く。**帳面**（装備・図鑑・地域・道標・設定・パス）は紙が地。混ぜない
- 一覧を行で組まない（地図・格子・盤面にする）
- 同じ幅のボタンを縦に積まない（主となる一手＋丸い衛星）
- 概念は自前の SVG 記号、世界に在る物は生成した絵
- 標準のナビバー・Form・List を出さない（**設定も含む**）

## 次にやること（優先順）

### 1. ユーザー作業（私からはできない）

- **App Store Connect にプライバシーポリシー URL を登録**
  `https://akao-onaga.github.io/WalkUP/privacy-policy.html`
  アプリ内リンクと同一でないと審査で指摘される
- **課金の再確認** — §7.4 の商品取得エラーが解消したか

### 2. ブラウザ版の残り（ユーザー方針：見本を完成させてから移植する）

アートだけで済む（指示があれば進められる）:
- ボスの追加キーフレーム6枚（攻撃・撃破 × 3体。`DESIGN.md` §10.1.1 の採用案）
- 主人公の装備差分（靴3段階なら3枚）
- パネルの四隅飾り

**決まった方針（2026-07-27）— 物語は「進行に紐づけた場面」で見せる**

道標から引く読み物ではなく、`sceneScreen` で場面として出す。背骨は
**街の人々を起こしていく**——人々が起きると街が活気を取り戻し、施設や機能が開放される。
活気ゲージ（§17.9）が物語の目盛りそのものになる。

**演出の半分はもう動いている。** `worldLayers` は活気で背景をクロスフェードし、
灯りを 12% / 45% / 75% で1つずつ点し、粒を暖色に変える。**足りないのは言葉だけ。**
場面は「背景が既に変わる瞬間」に置く。3地域 × 4節目（12/45/75/100%）= 12場面。

数字の裏取り: 章を全クリアで活気24（雑魚7×2＋ボス10）。100 までは周回で
雑魚38回 ≒ 38,000歩 ≒ 9.5日。**活気は章の進行では埋まらず、周回で埋まる長い目盛り。**

**実装済み（2026-07-27）**：12場面・3施設とも入っている（§6.1 / §6.2）。
第1章＝ウォークの軌跡（歩き方を靴に刻む）、第2章＝市（核の使い道）、
第3章＝掲示板（日替わりの残党）。施設は**地図の上に標として建つ**。
工房（靴の強化）は活気ではなく**チュートリアルで開く**。

次にやること:
1. **主人公の名前「ウォーク」をどこで名乗らせるか。** UI の名札は「主人公」のままにしてある
   （画面が先に名乗ると、物語で名前が出る場面が死ぬ）。**ただし施設「ウォークの軌跡」は
   第1章の活気45%で開く**ので、そこまでに名前が出ていないと施設名が初出になる
2. 12場面の文章を読み直す。**いまの文章は仮**で、背骨（街の人々を起こしていく）は
   通してあるが、章をまたぐ筋は通していない
3. 「世界の記述」（道標の読み物）を残すか決める。物語を場面へ移したので、
   道標に残るのは目撃とかけらだけ。22本は2日で尽きる

仕様の決定が要る（**数値か規則を1つ決めてもらう必要がある**）:
- 日替わりのダルモン残党の出現規則（§6）
- 歩行データ（時間帯・距離・階数）で出現を変える仕組み（§6）

判断が要る:
- 通知の許可を求める画面（§15-4 で「第1章クリア後」と決まっているので画面は作れる）
- 二言語化（ブラウザ版でやる価値は「英語で崩れないかの確認」だけ。全文翻訳のコストと見合うか）
- **「履き古した靴」の絵**（§11.1）。いまは記号で代替している。生成は1枚

### 3. Swift への移植（ブラウザ版が完成した後）

**`Theme.swift` にあるのは初期の部品だけ**（`Panel` / `InkButtonStyle` / `MeterBar` /
`StatTile` / `GameHeader`）。以降にブラウザ版で作った物は何も入っていない。
`web/README.md` の対応表に「未実装」と印を付けてある。部品から足すのが順当：
`world` / `night` / `crest` / `plate` / `ribbon` / `stepbar` / `pin` / `doll`。

**追加した21個のアセットはカタログに入っているだけで、Swift コードからの参照はゼロ。**

### 4. 未決事項（`DESIGN.md` §18.6）

- 「世界の記述」の総数（現在22本。**実測で第1章ぶんは2日で尽きる**）
- 「怠惰の核」の用途。§17.5 は「章をまたぐ強化の解放」と書いてあるが**実装が無い**。
  強化が使うのは澱だけなので、核は貯まる一方
- 活力パスを道標にも効かせるか（**効かせない方が良い**と考えている）

**決着済み**：設定画面は標準 `Form` を使わない（2026-07-27）。
「設定だけは OS の作法に従う」という当初の判断は取り消した。
Swift 側は `SettingsView` がまだ `Form` のままなので、移植の対象として残っている。

**決着済み**：携行品を廃止した（2026-07-27）。理由は次節。
抽選表は 記述65 / 目撃10 / かけら25 になった（`DESIGN.md` §18.4）。

## 踏んだ落とし穴（同じ轍を踏まないこと）

### 「決まっている」と思われている物が、一行しか書かれていないことがある

チュートリアルは §5.1 と §11-1 に**一行ずつ**あるだけで、
「第1章が兼ねる」「歩数ゼロでも1回は戦える」以外は何も決まっておらず、
**実装も無かった。** 歩数ゼロで起動すると章ゲートに届かず討伐ボタンが押せない。
§11 は「最重要」と書かれた節なので、そこが空でも気付かれないまま build 1 が TestFlight に上がっていた。

**「もう決まっているはず」と言われたら、実装を見て確かめること。**
仕様書に節があることと、中身があることは別。

### 数値で通る設計と、演出として通る設計は別

チュートリアルは「歩数ゼロで戦える」だけでは足りなかった。
実測すると **Lv1 素のままのノード1は勝率 61.9%** で、4割の審査員が最初の戦闘で負ける。
装備を渡して100%にし、さらに倒れる一撃だけを止めて必勝にした（§11.1）。

**「戦える」と「勝てる」は違う。** 仕様に「1回戦える」と書いてあっても、
その1回で負けたら対策として成立していない。

### 仕様書に書いてあっても、他の節と噛み合っているとは限らない

§18.4 は携行品を「戦闘前に使える回復」と定めていたが、§4.1 の戦闘は
**入る前に勝敗まで確定し、毎回 HP 全快で始まる。** 戦闘をまたいで HP が持ち越されないので、
**回復量をいくつに決めても何も起きない。** 半年ぶんの設計を積んだ後に気付いた。
結局この層は概念ごと落とした（2026-07-27）。

拾える物を足す前に、**使う場所が本当に存在するか**をコードで確かめること。
`GameState` に入れて画面に出すところまでは、噛み合っていなくても普通に作れてしまう。

### 引き切った時の逃がし先を、素材にしない

記述を引き切ったら かけら に振り替える実装になっていた。記述は数日で尽きるので、
**その日から抽選表の 65% がまるごと素材に化ける。** 「核1つに8.3日」の前提が
1日おきに崩れる。逃がし先はもう一方の読み物側（記述↔目撃）にし、
両方尽きた時だけ素材にする。

### 「ランダムに1体」が全個体を指しているとは限らない

目撃情報は `zako(章, 役割)` で引いていたが、この関数は**その章で同じ役割を持つ最初の1体**を返す。
第1章は ダラリ と アクビ が両方 standard なので、**アクビは道標では永久に目撃できなかった。**
図鑑が埋まらない理由が抽選ではなく引数にあった。個体を選ぶなら個体の一覧から選ぶこと。

### モードに追従させない（2026-07-27 に方針変更）

**アプリは紙の色に固定した。** OS のライト／ダークには追従しない（`DESIGN.md` §15-9.1）。
固定は **`Theme` / `WalkUPApp` / `project.pbxproj` の3か所**でやっている。
1か所でも外すと、シートやアラートなど OS が描く部分だけ反転する。

`.primary` / `.secondary` は**使わない**。`Theme.text` / `Theme.textSoft` を使うこと。
追従していた頃、背景を固定した画面で文字が消える事故を2回起こしている
（ペイウォールと戦闘画面）。

### 画像生成は毎回すべての属性を引き直す

ダラリの頭を直そうとして6回作り直し、毎回別の要素が壊れた（痩せる→紫になる→頭が戻る）。
**造形はプロンプト、明度と彩度は後処理**、と役割を分けて解決した（`tools/artpipeline`）。
`ART_PROMPTS.md` の「この文面は触らないこと」という注記は本気で書いてある。

### 「代替があるはず」と書いてあるだけで、実装されていないことがある

`web/README.md` は「絵が無い物は記号で代替し、画面が壊れないようにする」と書いていたが、
`itemArt` にその実装は無く、**絵を持たない装備を1つ足した瞬間に壊れた画像の印が出た**
（装備画面のスロットと一覧の両方）。いまは `onerror` で記号に差し替える。

絵は後から足せるが、**壊れた印は画面をそのまま壊す。**

### 「絵が切れている」の原因が絵とは限らない

実機で「主人公の頭が見切れている」と報告を受けたが、アセットにも
シミュレータの描画にも欠けは無かった。**被弾フラッシュ（`.brightness(0.5)`）で
髪の細い線まで明るく飛び、そこにダメージ数値が重なって**、頭が平らに欠けたように
見えていた。フラッシュを 0.3 に下げ、立ち絵に影を付け、数値は上端へ逃がした。
**演出は絵を壊す。** 立ち絵を差し替えたら、フラッシュ中の見え方も一度見ること。

### 保存データに焼き付いた文字列は、コードを直しても直らない

**2件やらかしている。** 図鑑の ID（役割ベース→種別ベース）と装備の表示名（杖→靴）。
どちらも `MasterData` を直せば済むと思ったが、既に保存された側は古いまま残る。
`GameStateStore.load()` に移行を通してある（`migratingLegacyBestiaryIDs` /
`refreshingEquipmentNames`）。**保存データに入る値を変えるときは移行を同じコミットで書く。**

図鑑の方は**画面が無かったので半日気付かなかった**。表示する場所が無いデータは、
壊れていても誰も気付けない。

### キャンバス内の「大きさ」はプロンプトで指定できない

ボスは STYLE SPEC を 70% → 85% に上げて生成したが、実測すると縦占有は 69%・46% で
雑魚（70%）と変わらなかった。**大きさは表示側のフレームで作ること**
（`BattleView` のボスは 250pt、雑魚は 175pt の高さ上限）。詳細は `ART_PROMPTS.md` §3.6 の表。
**立ち絵を左右に並べてからは幅も縛っている**（片側 168pt）。横長のマドロミが
主人公側にはみ出すため。

### 期待値だけで戦闘を設計してはいけない

紙の上では「辛勝」のはずが、実測すると勝率100%だった。
±10% の乱数は10ターンかけると平均に収束し、勝敗が決定論になる。
`WalkUPTests/BalanceSimulationTests.swift` が回帰テストとして常時走る。数値を変えたら落ちる。

### 実機で数日使わないと出ない不具合がある

「第1章を全クリアすると第2章の解放まで討伐ボタンが押せない」は、
ビルドもテストも通る状態で存在していた。シミュレータでは歩数を注入して一気に進めるため再現しない。
**ユーザーの実機報告が唯一の検出手段だった。**

### `.buttonStyle(.plain)` は中身のある場所しか反応しない

ノード行の余白を押しても無反応になっていた。`.contentShape(Rectangle())` が要る。

## ブラウザ版で踏んだ落とし穴（移植時にも効く）

### 装飾を足しても、構造が同じなら顔は変わらない

最初に「線と硬い影と不均一な角丸」を足して画集寄りにしたが、**縦積みのカードは
縦積みのカードのままで、健康アプリの顔から動かなかった。**
効いたのは構造の変更——アートを全面に敷いて UI をその上に浮かせる、一覧を地図にする、
同じ幅のボタンを縦に積むのをやめる。**色や書体の問題ではない。**

### 見た目の不満で始まった依頼に、忠実な移植を出してはいけない

「ブラウザで遊べるものを」と頼まれて既存画面を1:1で移植し、
**「僕の意図わかってます？」で止められた。** 不満の対象を複製した物が届いただけだった。
動機が現状の否定を含むときは、着手前に「何をどこまで変えてよいか」を確認する。

### 透過は「アルファで」と頼まない

`Fully transparent background (alpha channel)` と指定したら、
**透過を表す市松模様が画素として描き込まれた絵**が返ってきた。
必ず平坦な単色の塗りを要求し、後処理で抜く（`ART_PROMPTS.md` §3.7.2）。
キー色は素材に含まれない色を選ぶ（緑・琥珀の装備にはマゼンタ、灰紫の素材には緑）。

### 縁のにじみは救わず削る

キー色との距離に応じて縁を半透明にしたら、**混ざった画素が残って暗い背景の上で
マゼンタの輪郭として光った。** 余裕を持った距離で一気に抜き、さらに 1px 削る。
細かく淡い部分（砂粒）は削ると形が消えるので、そこだけ色の吸い出しで対処する。

### 16:9 の絵を縦画面に敷き詰めると構図が壊れる

章の扉絵を `cover` で全画面に敷いたら 2.4 倍に拡大され、
**小さく立たせた主人公が巨大になった。** 額に入れた帯として見せる形に直した。
横長の物を正方形キャンバスに置くのも同様で、上下の余白が画面上の隙間として焼き付く
（`cutout.py --tight` を使う）。

### 絶対配置は静的配置の兄弟より上に描かれる

ホームの背景（絶対配置）が中身より手前に来て、画面全体が霞んだ写真の下に沈んだ。
また、画面の中で `z-index` を持つ部品が上に重ねたシートを突き抜けた。
**`.screen` に `z-index: 0` を、`.body` に `position: relative` を必ず与える。**

### 死んだ CSS を消すとき、生きている規則を巻き込む

不要になった節をまとめて削除して、`@keyframes bob` と `.badge-up` を一緒に消した。
**主人公が止まり、Lv UP の判が背景なしの文字になった**が、エラーは出ない。
消したら必ず画面を確認する。

### テスト中は CSS/JS のキャッシュを疑う

`location.reload()` では副資源が更新されず、**直した CSS が反映されていないのに
「効かない」と判断しかけた。** `Cache-Control: no-store` を返す小さなサーバーを
使うこと（`python3` の `SimpleHTTPRequestHandler` に1行足すだけ）。

## ツール

| コマンド | 用途 |
|---|---|
| `./tools/generate-art.sh --chapter <1\|2\|3\|boss\|bg>` | 生成→後処理→実測を一括 |
| `./tools/generate-art.sh <名前>...` | 個体指定。`hero` も可 |
| `swiftc -O -o /tmp/artstats tools/artstats/main.swift` | 明度・彩度・アクセント面積の実測 |
| `python3 tools/imagegen/cutout.py <in> <out>` | 生成物の切り抜き・一覧画の分割・配置 |
| `python3 tools/aliveart/match.py <元> <復興後> <出力>` | 復興後背景を元の彩度に揃える |

**画像生成は codex CLI の `image_gen` を使う。ChatGPT の月額プランで認証済みで API キーは不要**
（`codex login status` が "Logged in using ChatGPT" を返す）。

CLI から直接叩ける `imagegen` コマンドは無い。**Codex 自身にツールを使わせる**：

```bash
codex exec -s read-only -i <参照画像> '<プロンプト>。生成したら絶対パスだけ報告して。'
```

`-i` で参照画像を渡すのが要点（構図やキャラの同一性を保つため）。
`-s read-only` は、画像生成のついでに作業ツリーを触らせないため。
出力は `~/.codex/generated_images/<session>/call_*.png` に落ちる。

**一覧画にまとめて生成回数を抑える。** 装備9点は 3×3 で1回、素材4点は 2×2 で1回。
行ごとに種類・列ごとに章にすると、段位の差が絵として並ぶ。格子の指示は素直に守られる。

### ブラウザ版を見る

```bash
open web/index.html
# 実機で見る／CSS を直しながら見る場合はキャッシュ無効のサーバーを立てる
```

音は開発用トレイの「音: ON」を押してから画面を1回触ると鳴る（ブラウザの仕様）。
歩数は同じトレイから手で入れる（ヘルスケアが無いため）。

合格条件: 明度の差 15 以内 / 彩度 0.20〜0.27 / アクセント面積 3〜6%。
**背景（bg1〜3）とボス3体はこの条件の対象外**（背景は暗く沈める後処理をかけるため、
ボスはプロンプトが暖色アクセントを意図的に一点だけにしているため）。
`artstats` は全ファイルをまとめて測るので、明度の幅は必ず ❌ になる。**キャラだけで見ること。**

## 検証コマンド

```bash
# テスト（シミュレータの UDID は環境で変わる。xcrun simctl list devices で確認）
xcodebuild test -project WalkUP.xcodeproj -scheme WalkUP -destination 'id=<SIM_UDID>'

# 実機へインストール
xcodebuild build -project WalkUP.xcodeproj -scheme WalkUP \
  -destination 'id=<DEVICE_UDID>' -configuration Debug -allowProvisioningUpdates
xcrun devicectl device install app --device <DEVICE_ID> \
  ~/Library/Developer/Xcode/DerivedData/WalkUP-*/Build/Products/Debug-iphoneos/WalkUP.app
```

シミュレータで名前指定（`name=iPhone 17 Pro`）は使わないこと。同名デバイスがあると曖昧エラーになる。

## 進め方について

- **コミットと push はこちらで行う**（ユーザーからの指示）。作業ツリーを溜めない
- ユーザーは実機で触って不具合を報告する。**「詰まった」報告は仕様かバグかを切り分けて答える**
- 同じリポジトリで複数の Claude セッションを走らせない。過去に別セッションが
  `git add -A` で未検証コードを巻き込んで push した事故があった
- **デザインの作業は「まず方針から」。** ユーザーは着手前に方向を決めたがる
  （「まず方針を決めましょう」と2度言われている）。選択肢を並べて選ばせてから実装する。
  ただし決まった後は最後まで通すこと
- 画像生成はユーザーの ChatGPT アカウントで費用が発生する。**量産は指示を取ってから。**
  1枚試して構図と色を確かめ、通ったら残りを回す
